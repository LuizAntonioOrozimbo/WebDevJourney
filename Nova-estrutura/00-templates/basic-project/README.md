# Basic Project Template

Este é o template base para iniciar qualquer novo projeto HTML, CSS e JavaScript dentro do repositório **WebDevJourney**.

Ele fornece uma estrutura organizada, limpa e profissional, para facilitar a criação de novos projetos sem precisar começar do zero.

---

## 📁 Estrutura do template


basic-project/
├── index.html
├── style.css
├── app.js
├── assets/
│ └── img/
│ └── .gitkeep
└── README.md



### **index.html**
Estrutura HTML inicial com:
- `<!DOCTYPE html>`
- `meta charset="UTF-8"`
- `meta viewport`
- link externo para CSS
- script para `app.js`
- layout básico com `<header>`, `<main>` e `<footer>`

### **style.css**
Arquivo organizado com:
- Reset básico
- Variáveis CSS
- Estrutura padrão de layout
- Comentários para cada bloco

### **app.js**
Arquivo JavaScript com:
- Função inicial (`init()`)
- Exemplo de evento
- Estrutura limpa e pronta para expansão

### **assets/img/**
Local para guardar imagens do projeto.  
O arquivo `.gitkeep` mantém a pasta versionada mesmo vazia.

---

## 🚀 Como usar este template

1. Copie a pasta `basic-project/`
2. Cole no diretório desejado, como:


03-my-projects/production/

3. Renomeie a nova pasta usando **kebab-case**, por exemplo:

meu-projeto-timer


4. Edite:
- `index.html` (título, conteúdo inicial)
- `style.css` (cores, tema)
- `app.js` (lógica do projeto)
- `README.md` (descrição do seu projeto)

---

## 🧩 Recomendações

- Mantenha o padrão HTML/CSS/JS separado
- Coloque imagens sempre dentro de `assets/img/`
- Adicione pastas extras somente se necessário (`fonts/`, `videos/`, etc.)
- Atualize o README do novo projeto para descrevê-lo bem

---

## 📦 Exemplos
Se quiser, posso gerar:
- Um template avançado  
- Template responsivo  
- Template com módulos JS  
- Template com páginas múltiplas  

Basta pedir.

